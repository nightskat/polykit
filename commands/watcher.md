---
description: Watcher — diff model/version vendor, alert 1 lần khi đổi
allowed-tools: Bash
---
Chạy dry-run (không gửi alert) và hiển thị changes:
```
python3 "${CLAUDE_PLUGIN_ROOT}/bin/watcher.py" --dry-run
```
