---
description: Quota failover — pressure→ping trước, cap→ping reactive, lỗi lạ→im
argument-hint: "[--pressure N | --stderr-file PATH]"
allowed-tools: Bash
---
Chạy với tham số từ `$ARGUMENTS`:
```
python3 "${CLAUDE_PLUGIN_ROOT}/bin/failover.py" $ARGUMENTS
```
