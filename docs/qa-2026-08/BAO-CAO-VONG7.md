| Mục | Lệnh + Kết quả |
|---|---|
| Vá lỗi text mode (lỗi) | Lệnh: `printf hi \| FAKE_OC_FAIL=1 PATH="/tmp/pk-fakebin:$PATH" ~/.pyenv/versions/3.11.8/bin/python bin/dispatch.py opencode --no-traps --allow-unknown-model --timeout 5`<br>Output: `[polykit] warning: vendor 'opencode' không nhận cờ model, đang chạy mặc định của chính nó, không xác định được slug.`<br>`ERROR: opencode failed with exit code 1`<br>`Warnings:`<br>`  - boom: fake fail`<br>Mã thoát: **1** |
| Vá lỗi `--result-json` | Lệnh: `printf hi \| FAKE_OC_FAIL=1 PATH="/tmp/pk-fakebin:$PATH" ~/.pyenv/versions/3.11.8/bin/python bin/dispatch.py opencode --no-traps --allow-unknown-model --timeout 5 --result-json`<br>Output: `[polykit] warning: vendor 'opencode' không nhận cờ model...`<br>`{ "status": "error", "vendor": "opencode", "warnings": ["boom: fake fail", "vendor 'opencode' không nhận cờ model..."], ... }`<br>Mã thoát: **1** |
| Revert in trùng (Test đỏ) | Sửa file tests, thêm `test_vong7_text_duplicate_warning` đếm số lần báo lỗi (mong đợi = 1). Khi revert mã (không xoá warning khỏi array), test báo failed do `count == 2`. |
| Ví dụ 1: `doctor.py` | Lệnh: `python3 bin/doctor.py`<br>Output: Bảng 7 vendor có `verify_cmd` (codex, gemini, claude, grok, agy, openrouter, dsh), các vendor khác không có `verify_cmd` nên không hiển thị trong bảng doctor mặc định (exit 0).<br>Mã thoát: **0** |
| Ví dụ 2: `dispatch.py` | Lệnh: `printf "hi" \| python3 bin/dispatch.py codex --result-json`<br>Output: `[polykit] ⚠ traps for codex:` ... `{ "status": "ok", "vendor": "codex", "model": "gpt-5.6-terra", "stdout": "Hi! What would you like to work on?\n", "exit_code": 0 }`<br>Mã thoát: **0** |
| Ví dụ 3: `failover.py` | Lệnh: `python3 bin/failover.py --pressure 85`<br>Output: `{"action": "ping_proactive", "signal": "pressure", "message": "⚠️ Claude còn ~15% (pressure 85%). Handoff sang đâu? codex / gemini / để cap", "notified": true}`<br>Mã thoát: **0** |
| Ví dụ 4: `watcher.py` | Lệnh: `python3 bin/watcher.py --dry-run`<br>Output: `[DRY RUN] 🔔 polykit: agy ready→auth_unverified; agy -14 models` và object JSON mô tả thay đổi.<br>Mã thoát: **0** |
| Ví dụ 5: `pytest` | Lệnh: `python3 -m pytest tests/ -q`<br>Output dòng cuối: `152 passed in 11.53s`<br>Mã thoát: **0** |
